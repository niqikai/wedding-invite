import{timingSafeEqual}from"crypto";import{NextRequest,NextResponse}from"next/server";import{createAdminToken}from"@/lib/auth";export async function POST(req:NextRequest){const given=String((await req.json().catch(()=>({}))).secret||"");const actual=process.env.ADMIN_SECRET||"";const valid=given.length===actual.length&&actual.length>=12&&timingSafeEqual(Buffer.from(given),Buffer.from(actual));if(!valid)return NextResponse.json({error:"Unauthorized"},{status:401});const res=NextResponse.json({ok:true});res.cookies.set("wedding_admin",await createAdminToken(),{httpOnly:true,sameSite:"strict",secure:process.env.NODE_ENV==="production",maxAge:43200,path:"/"});return res}
