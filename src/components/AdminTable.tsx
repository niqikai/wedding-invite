"use client";import{useState}from"react";type Row={id:string;name:string;attending:boolean;partySize:number;dietary:string|null;message:string|null;createdAt:string};export function AdminTable({rows}:{rows:Row[]}){const[q,setQ]=useState("");const[filter,setFilter]=useState("all");const shown=rows.filter(r=>r.name.toLowerCase().includes(q.toLowerCase())&&(filter==="all"||(filter==="yes")===r.attending));return <section className="tableWrap"><div className="tools"><input aria-label="搜索姓名" placeholder="搜索姓名" value={q} onChange={e=>setQ(e.target.value)}/><select aria-label="筛选状态" value={filter} onChange={e=>setFilter(e.target.value)}><option value="all">全部状态</option><option value="yes">出席</option><option value="no">缺席</option></select></div><div className="tableScroll"><table><thead><tr><th>姓名</th><th>状态</th><th>人数</th><th>饮食忌口</th><th>留言</th><th>提交时间</th></tr></thead><tbody>{shown.map(r=><tr key={r.id}><td>{r.name}</td><td>{r.attending?"出席":"缺席"}</td><td>{r.partySize}</td><td>{r.dietary||"—"}</td><td>{r.message||"—"}</td><td>{new Date(r.createdAt).toLocaleString("zh-CN")}</td></tr>)}</tbody></table></div></section>}
