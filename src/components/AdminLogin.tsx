"use client";import{FormEvent,useState}from"react";import{useRouter}from"next/navigation";export function AdminLogin(){const[e,setE]=useState("");const router=useRouter();async function submit(x:FormEvent<HTMLFormElement>){x.preventDefault();const secret=new FormData(x.currentTarget).get("secret");const r=await fetch("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({secret})});if(r.ok)router.refresh();else setE("密码不正确")};return <main className="login"><form onSubmit={submit}><p>PRIVATE ACCESS</p><h1>RSVP Admin</h1><label>管理密码<input name="secret" type="password" required autoFocus/></label><button className="submit">进入</button>{e&&<p className="error">{e}</p>}</form></main>}
